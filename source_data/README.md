# Source Data

Large or raw molecular datasets are not tracked in this repository.

To run the current CHAMANP pipeline, provide the COCONUT source CSV at:

```text
source_data/coconut_05-2025.csv
```

The repository tracks `coconut_taxonomy.json` as a lightweight taxonomy reference, but the raw CSV must be supplied by the user.
